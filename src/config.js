/**
 * TODO: In the future we need ability to switch between different configs
 *
 */

const development = {
  OrbitDbEventLog: 'LABOR-X-DEV',
  LogLevel: 'debug',
}

const ropsten = {
  OrbitDbEventLog: 'LABOR-X-ROPSTEN',
  LogLevel: 'debug',
}

export default development